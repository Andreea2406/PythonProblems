#Write a function with a single parameter that will display the last even digit of it. If the number does not contain any even number, it will display "-1".
def my_function(n):
    ok=0
    while n!=0:
        if(n%10)%2==0:
            print(n%10)
            ok=1
            break
        else:
            n//=10
    if ok==0:
        print(-1)
